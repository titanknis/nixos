{
  config,
  lib,
  pkgs,
  username,
  ...
}:
{
  # virtualisation.docker = {
  #   enable = true;
  #   # Enable Docker's "daemon" service
  #   enableOnBoot = true;
  # };
  # users.users.${username}.extraGroups = ["docker"];
}
